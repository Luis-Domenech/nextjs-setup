import { LocaleKeys } from "..";
import { Locale, ComponentName } from "../../utils/types";

export const Home: Record<Locale, typeof LocaleKeys[ComponentName.home]> = {
  es: {
    title: "App"
  },
  en: {
    title: "App"
  }
}