import { Locale, LocaleName } from "./types"

export const default_locale: Locale = Locale.es
export const locales: Locale[] = [Locale.es, Locale.en]

export const LocaleToName: LocaleName = {
  en: "English",
  es: "Español"
}